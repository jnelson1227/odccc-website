node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
.env
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# Jill's full-resolution photo drops, staged here while picking shots for
# public/images. Gigabytes of originals, and GitHub rejects any file over
# 100MB — so never let `git add -A` sweep them into a commit.
/Final Carvings Chainsaw 2026/
/Social Sharing Chainsaw 2026/

# Carver roster exports. These carry phone numbers, emails and mailing
# addresses for every carver — only the bios and links belong on the site,
# and none of this belongs in a public repo.
/2026 CARVERS-Grid view.xlsx
/2026 CARVERS-Grid view.csv
/~$*.xlsx
